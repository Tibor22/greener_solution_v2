import { FC } from 'react';
import { IconType } from 'react-icons';

interface Props {
	location: string;
	data: string;
	icon: IconType;
}

const Widget: FC<Props> = (props): JSX.Element => {
	return <div></div>;
};
